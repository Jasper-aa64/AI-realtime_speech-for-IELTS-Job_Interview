# SPEC — 实验2~5 实验报告生成(四份 docx,重写加长,格式对齐实验1)

> 给 Codex(出图)+ Sonnet(成文/重写)。今晚交付 **实验2、3、4、5 四份 Word 报告**。
> 指挥(本会话)定边界与格式;不替用户跑实测/不碰 key。
> **实验1不重做**,只作格式样板参考。

## 0. 总目标(已升级:重写加长,这是期末评分件)
今晚产出四份 `.docx`,全部**逐项对齐 `软件构造_实验1_报告.docx` 的格式**,且**每份大幅加长重写**:
- `软件构造/软件构造_实验2_报告.docx`
- `软件构造/软件构造_实验3_报告.docx`
- `软件构造/软件构造_实验4_报告.docx`
- `软件构造/软件构造_实验5_报告.docx`(已有草稿,**推倒重写并加长**,不是只润色)

### ⚠️ 最高优先:篇幅(评分硬指标)
- 现有四份内容**太短,不合格**。Sonnet 要**全部重写、尽可能写长写满**,不是小修。
- **每份正文目标 ≥ 12000 字**(中文),到 15000+ 更好。对标并超过实验1(约1.9万字)的充实度。
- 加长靠**实质内容**,不是车轱辘话。每个论点展开:是什么 → 为什么这么做 → 怎么实现 → 代码/结构证据 → 权衡与替代方案 → 收益与局限。多用表格、列表、代码清单、图,把每章写厚。
- 取消"只润不动"限制:**允许重写、扩写、补充论述、增加小节**。

## 1. 格式硬要求(四份共同,最高优先)
**做法:以实验1的 docx 为模板克隆,不要手画封面。**
1. 复制实验1的封面表格 XML、`word/styles.xml`、`word/settings.xml`、sectPr(页边距 `top/bottom=1417`、`left/right=1587`、`header/footer=720`、**无页眉页脚**)。
2. 把各实验正文灌进模板正文区。
3. **不要**自创蓝底表格 / footer(实验5草稿现有蓝底 `D9EAF7`/`1F4E79` 和 `footer1.xml`,必须删)。

### 封面字段(每份只改"实验编号/名称",其余沿用实验1)
| 字段 | 共同值 |
| --- | --- |
| 抬头 | 计算机与信息学院实验报告 |
| 实验课程 | 软件构造 |
| 学号 | 23111302079 |
| 姓名 | 梁峻铭 |
| 班级 | 23软件工程 |
| 指导教师 | 李文杰 |
| 实验室 | 2060201 |
| 实验日期 | **留给用户确认,别擅自编** |

| 报告 | 实验编号 | 实验名称(拟,用户可改) |
| --- | --- | --- |
| 实验2 | 2 | 利用设计模式进行类设计并编程实现 |
| 实验3 | 3 | 测试驱动开发(TDD) |
| 实验4 | 4 | 软件重构 |
| 实验5 | 5 | 复用的软件资源进行软件构造 |

### 字体/表格(对齐实验1)
- 封面标签字:Times New Roman + 宋体,sz=28,kern=2;名称/教师值:宋体 sz=26。
- 表格:细黑实线边框(single sz=4),**无底色**。
- 一级标题"一、二、三…"沿用实验1标题样式;正文 Times New Roman + 宋体。

## 2. 各报告内容来源 + 章节(对齐实验1风格,每章写厚)

> 统一骨架(实验1风格):一、实验目的;二、实验环境;三、实验内容/原理;四~N、核心展开;末、测试与验证;末、实验结论与心得。**每章都要展开论述,不写提纲。**

### 实验2 — 设计模式(来源:`实验2_设计文档.md` + `实验2_类图.puml`)
- 核心:Strategy / Adapter / Chain of Responsibility / Template Method 四模式在 `apps/ai/provider_adapters.py` 的真实落点。
- 必含且展开:每个模式的 GoF 定义/意图/UML结构/适用场景/本项目落点/代码证据(`AIProvider`/`BaseProviderAdapter`/`AiTaskTemplate`/`ProviderChain`)/与其它模式协同/替代方案权衡;四模式依赖闭环;目标类结构;重构前问题 vs 模式引入后改善。
- **配图**:类图(渲染 `实验2_类图.puml`)、四模式协同关系图、时序图。

### 实验3 — TDD(来源:`实验3_TDD验证报告.md`)
- 核心:Red/Green/Refactor 全过程;C++ audio_core characterization tests、libfvad backend tests、browser metrics tests、Django regression。
- 必含且展开:TDD 理论与意义、测试金字塔、每类测试的用例设计表与断言、Red→Green→Refactor 每步实录、一键验证脚本、防止的具体回退、测试覆盖与结果分析、TDD 的收益与局限。
- **配图**:Red-Green-Refactor 循环图、测试分层/金字塔图。

### 实验4 — 重构(来源:`实验4_重构报告.md`)
- 核心:audio_core 纯核抽取、Strategy 隔离 VAD backend、Adapter 暴露 WASM 边界、baseline+WASM 可选层。
- 必含且展开:重构定义与时机、坏味道识别、重构前/后结构对比、每个重构手法(提炼类/提炼函数/引入策略/引入适配器等)的动机与步骤、关键代码前后对比、重构收益(可测/可复用/可部署/风险)、如何用测试保护重构。
- **配图**:重构前 vs 后架构对比图、模块依赖图、类图。

### 实验5 — 复用(来源:已有 `软件构造_实验5_报告.docx` 草稿,十五章)
- **推倒重写并加长**(不是只改格式)。保留草稿的真实技术事实(libfvad BSD-3、howler.js MIT、Emscripten、ExaminerAudioPlayer、stable=1/afconvert、撕裂实为蓝牙耳机),但**重新组织、大幅扩写**到 ≥12000 字。
- 展开:软件复用理论与层次、选型对比与许可证合规、为什么复用而非手搓(用真实踩坑论证)、两条复用链路集成架构、关键实现、测试验证、问题与解决全过程、复用收益量化、结论心得。
- **配图**:libfvad→audio_core→WASM 复用链路图、howler.js→ExaminerAudioPlayer 播放链路图。

## 3. 配图(Codex 出,mermaid/PlantUML/PNG 均可)
- 用 Codex 绘图工具生成后插入 docx。图风格服从实验1(图下居中题注"图 N XXX图")。
- 实验2类图优先复用现成 `实验2_类图.puml`。

## 4. 红线(四份共同)
- **唯一硬红线:不许伪造"已实测"的关键指标**(保分不是限制——老师查出编造数据会重扣)。
  - 真实测过的照写;**没实测的用文字论述/估算/定性描述,但不得谎称"实测得到 X"**。
  - `P50 4.66s / 1.46s / 2.11s` 是历史 HTTP 端点数字,**不是 Aiapis gpt-5.4-mini 新路由成绩**;引用必须注明"历史端点对照数据",不得冒充本次实测。
  - 行数等可现查的(howler+路由改造后已变):能查就写实数,查不到就定性描述,别编精确数。
  - **除关键实测指标外,论述/背景/原理/设计权衡/收益分析可放手写长**——这是评分主体,尽量充实。
- 许可证准确:libfvad=BSD-3,howler.js=MIT。
- 不提交 db.sqlite3;不泄露任何 key。实验1不动。

## 5. 验收(用户)
- [ ] 四份 docx 各能用 Word 打开,**第一页封面与实验1逐字段逐样式一致**(无蓝底、无页脚、字体/边框/页边距对齐)。
- [ ] **每份正文 ≥ 12000 字**,章节齐全、论述充实、配图到位。
- [ ] 数字真实或定性,无伪造"已实测"。
- [ ] 实验编号/名称正确,封面日期为用户确认值。

## 6. 建议执行顺序(今晚赶工)
1. 先抽实验1的"封面+styles+sectPr"做成可复用模板,四份共用。
2. 实验5 先收(已有草稿,重写加长+配图)。
3. 实验2/3/4 用 md 底稿重写加长 → 套模板 → 配图。
4. 每份出完跑格式 + 字数自查(§1/§5),再交用户。
