> ⚠️ **本文件已作废(SUPERSEDED)**。需求已从"只润不动"改为"四份报告全部重写加长"。
> 以 `docs/SPEC-exp2to5-reports-generation.md` 为准。本文件仅留作历史参考。

# SPEC — 实验5报告润色(Sonnet 执行,只润文字不动数据)【已作废】

> 给润色方(Sonnet)。GPT 已出草稿 `软件构造/软件构造_实验5_报告.docx`(正文约 6.3 万字、十五章)。
> 用户决定:**只润文字,不动数据**。指挥(本会话)定边界,不替用户跑实测/不碰 key。

## 唯一最高红线:不碰任何数字
- **不得修改、增加、删除、推算任何数值**:延迟(s/ms)、token 数、代码行数、文件大小(KB)、测试数量、页数、百分比、版本号、学号等。
- 数字保持草稿原样,逐字不动。需要核对/更正数字是用户实测后的事,不是润色范围。
- 也**不要**自作主张把可疑数字改成"待测"——那也是动数据。润色只碰文字。

## 润色范围(只做这些)
1. **语言**:中文表达流畅、术语统一(libfvad / howler.js / Emscripten / WASM / Aiapis / gpt-5.4-mini 等大小写与写法一致)、去口语化、去重复啰嗦。
2. **结构**:段落衔接、过渡句、章节内逻辑顺序;十五章标题层级保持与实验1报告一致的编号风格。
3. **可读性**:长句拆分、被动改主动、列表/表格表述对齐。
4. **格式一致性**:与 `软件构造_实验1_报告.docx` 的字体(Times New Roman + 宋体)、标题样式、表格风格对齐;不破坏现有图/表/代码块。

## 不做
- 不动数字(见红线)。
- 不增删章节、不改技术结论、不改许可证信息(libfvad=BSD-3,howler.js=MIT)。
- 不引入新的"实测数据"或案例。
- 不泄露任何 key;不提交 db.sqlite3。

## 交付
- 覆盖写回 `软件构造/软件构造_实验5_报告.docx`(.docx,保持可被 Word 打开、十页+ 正常渲染)。
- 润色后回报:改了哪些类别(语言/结构/格式),确认"数字零改动"。

---

## ⚠️ 格式硬要求:封面与整体样式必须与实验1**完全一致**(用户重点)

当前实验5报告封面是**自创的蓝底表格**(底色 `D9EAF7`/`1F4E79`),还多了 `word/footer1.xml`,**与实验1完全不同 → 不合格**。实验1封面是无底色的标准学院表格。

### 最可靠做法:克隆实验1当模板,别手画
**不要照着重画封面**(必偏)。正确做法:
1. **以 `软件构造_实验1_报告.docx` 为模板**:复制它的 `word/document.xml` 封面表格那一整段(从 `计算机与信息学院实验报告` 到封面表格结束)、`word/styles.xml`、`word/settings.xml`、sectPr(页边距 `top/bottom=1417`、`left/right=1587`、`header/footer=720`、**无页眉页脚**)。
2. 把实验5的正文(十五章)灌进这个模板的正文区。
3. **删掉实验5自创的 `footer1.xml` 和蓝底表格样式**——实验1没有页脚、封面无底色。

### 封面字段精确值(照实验1格式填,只改实验5相关项)
| 字段 | 值 |
| --- | --- |
| 抬头(居中标题) | 计算机与信息学院实验报告 |
| 实验课程 | 软件构造 |
| 实验编号 | 5 |
| 实验名称 | 复用的软件资源进行软件构造 |
| 学号 | 23111302079 |
| 姓名 | 梁峻铭 |
| 班级 | 23软件工程 |
| 指导教师 | 李文杰 |
| 实验室 | 2060201 |
| 实验日期 | 留空或填用户确认日期(今天 2026.5.31,**待用户确认,别擅自编**) |

> 学号/姓名/班级/指导教师/实验室沿用实验1原值(同一作者同一课程)。**实验名称用"复用的软件资源进行软件构造"**(用户给的实验五主题),不要写成旧的"开源复用与WASM方案"。

### 封面字体(与实验1逐项对齐)
- 标签字(实验课程/编号/…):`Times New Roman` + `eastAsia 宋体`,`sz=28`,`kern=2`。
- 实验名称、指导教师值:`宋体`,`sz=26`。
- 表格:细黑实线边框(`single sz=4`),列宽 `1788/794/4574`,行高 `680`,水平居中。

### 正文格式(与实验1对齐)
- 一级标题"一、二、…"沿用实验1的标题样式;正文 Times New Roman + 宋体。
- 表格统一成实验1的**无底色细黑边框**风格,去掉蓝底填充。
- 图/代码块保留,但风格服从实验1。

### 格式验收(submit 前自查)
- [ ] 第一页封面与实验1**逐字段、逐样式**一致(抬头、表格结构、字体、无底色、无页脚)。
- [ ] 无 `footer1.xml`(实验1无页脚)。
- [ ] 页边距 = 实验1(1417/1587)。
- [ ] 正文表格无蓝底,统一细黑边框。
- [ ] Word 打开渲染正常,十页+。

> 实现提示:Python 直接改 docx 的 XML 风险高;更稳的是用 `python-docx` 或直接以实验1的 docx 为基底文件,编辑其正文。无论哪种,**封面那段 XML 直接从实验1拷,不要重写**。

---

## 附:报告内数字清单(给用户,不是给 Sonnet 改 —— 供你 submit 前自查真伪)

指挥已扫报告,数字分三类:

**A. 已验证、可放心(我或你此前真测过)**
- `audio_core_wasm.js 68KB` / `.wasm 144KB` —— 指挥实测属实(68350 / 144422 字节)。
- `.wasm` MIME = `application/wasm` —— 此前验过。
- `max_tokens=80`(follow-up)—— 代码配置值,属实。
- 许可证 libfvad BSD-3 / howler.js MIT —— 属实。

**B. ⚠️ 可能过时(howler 重写后变了,报告可能还是旧值)**
- `app.js 8776 行` —— 现已远大于此(重写+howler 后,文件已 343KB)。**submit 前请重数。**
- `services.py 4139 行` —— AI 路由改造后可能已变。**请重数。**
- 测试数量(报告若写具体数)—— 现约 290 个 `test_`,以最新为准。

**C. 🔴 未验证、有编造风险(最高优先核对)**
- `P50 4.66s / 1.46s / SSE 2.11s` —— 这些是**此前另一个端点**的历史数字,**不是 Aiapis gpt-5.4-mini 新路由实测值**。AI 路由真人实测还没跑(key 未注入)。**若报告把它们当作本次 Aiapis 路由的成绩写,就是编造**——务必跑完 `validate_speaking_ai_routing.py` 拿真实 latency/usage 后替换,或在报告里明确标注"数据来源:历史 HTTP 端点对照,Aiapis 路由实测待补"。

> 建议顺序:先让 Sonnet 润文字(数字冻结),你这边并行跑 AI 路由实测 + P1 听感;实测数字回来后,由你/Sonnet 做一次"仅数字"的定点更新,避免润色与数据互相返工。
