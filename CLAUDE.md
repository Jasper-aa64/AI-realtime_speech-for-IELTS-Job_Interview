# Project map (for Claude Code)

Claude Code reads `CLAUDE.md`; Codex and other agents read `AGENTS.md`. To keep a
single source of truth, this file imports the map instead of duplicating it.

@AGENTS.md

Navigation hub for all other docs: `docs/index.md`.

Before starting / restarting / exposing the stack, read
`docs/ops/windows-startup-and-public-access.md` — the steady state and the
do-not-start rules live there, and getting them wrong breaks public access.
